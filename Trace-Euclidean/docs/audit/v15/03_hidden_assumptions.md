# Hidden assumptions and object scope

1. GlobalLatticePresentation bundles a coded number field, total reality, positive rank, full projective lattice, positive definite form, and integrality. GlobalLatticeClass uses field equivalence and semilinear quadratic-lattice isometry. AUTHOR_CONFIRMATION_REQUIRED for the intended cross-field convention.
2. V15 global theorems use each class's degree as its strict trace threshold. Inherited v9 fixed-threshold theorems are supporting or historical results only.
3. V15OdlyzkoTable4Input is an external theorem premise. For a totally real field, the cited row gives |D_F| > 36.347^d exp(-10.667). Lean proves the numerical conversion and rank-one integrality; no project axiom asserts the table.
4. The rank-one endpoint quantifies over every nonzero fractional ideal and totally positive coefficient satisfying alpha I^2 subset O_F. GlobalLatticePresentation.rankOne_ideal_bridge_at now constructs this presentation from every canonical rank-one global lattice and preserves the trace condition at any real threshold. The older rankOne_ideal_bridge specializes to threshold 2, while rankOne_ideal_bridge_degree uses the manuscript's degree threshold. AUTHOR_CONFIRMATION_REQUIRED remains for the paper-to-model and cross-field equivalence conventions.
5. An actual reduced Z-basis is constructed from the ideal. The determinant, parity, trace/norm/index, and strict covering conditions are derived for that basis rather than assumed at the final endpoint.
6. V15SixFreeIsometryClass requires an O_F-linear equivalence preserving the quadratic form. Both directions are proved. All six representatives satisfy the stated positivity and integrality conditions and have constructive strict covers.
7. The general reduced-binary covering-radius formula is exported for reduced trace Gram forms in the project model (`2 <= A <= C`, `2B <= A`) and transferred to actual fractional ideals. The two displayed integer-scalar formulas use the full quadratic integer ring and are bundled with the general clause in v15_proposition_six_one_full. The classification proof continues to use its direct rational deep hole and six constructive covers.
8. The p-norm endpoint represents finite p >= 1 and p = infinity by separate constructors. It averages all complex embeddings, whose values are real for a totally real ground field. Their count equals the field degree. The final finite-class theorem still carries the explicit Odlyzko Table 4 premise.
