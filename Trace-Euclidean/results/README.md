# Result files by author version

- `v15-release-summary.json` is the active scoped Grade B release index.
- `python_v15_summary.json` and `v15-mathematica-transcript.txt` are the
  public v15 computational reruns.
- `summary.json`, `tests.json`, `verification_ledger.json`, and the v9 manual
  belong to the historical v9 source. Their source hash differs from v15.

The private source-bound checks are described in the v15 audit. The author
manuscript and its private computation directory are not part of this package.
